# Clean code!

## Имена переменных

1. Шум
```
  //БИК банка получателя
  payeeBankBIC
```

2. Недостоверные комментарии
```
// методы фингерпринта (объект ...) записываются как свойство в window
```
устаревшая информация, и все уже работает по другому, через стороннюю библиотеку

3. Закомментированный код, удалил не используемый метод
```
закомментированные тесты
```

4. Шум
```
// Set up the interval.
а ниже функцияс с таким же названием :)
```

5. Шум
// ИНН получателя
payeeINN

6. Шум
// <script> для ... добавляется через loadScript
через пару строчек ниже видно, как используется импортированный метод loadScript

7. Шум
 // КПП получателя
  const payeeKPP

8. Закомментированный код, удалил не используемый метод
```
// const handleAgreementToDataProcessing = (): void => {
```

9. Неочевидные комментарии
// Добавить правильный тип для метода
А смысл в том, что она автоматически выводится, и не понятно зачем это вообще нужно)

10. Слишком много информации
```
// Необходимо добавлять валидации для полей в порядке их нахождения на форме, для корректного скролла к первому элементу с ошибкой
```
```
// Соблюдайте порядок валидации и нахождения полей на форме одинаковыми
```

11. Недостоверная информация
// удалить, после починки метода бекендом
метод уже починили, и не ясно зачем это удалять, если все работает :)

12. Нелокальная информация
```
//здесь патчится глобальный обьект виндов
Object.defineProperty(window, 'tNPS', {
        configurable: true,
        enumerable: true,
        get: function () {
лучше писать это, переl функцией, при ее обьявлении, а не в коде
```

13.Нелокальная информация
```
// патчит глобальный обьект window
 loadScript(scriptUrl).then(() => {
      const tid: string = w.AFCYBERTONICA.init('')

лучше писать это, перед функцией, при ее обьявлении, а не в коде
```

14.Закомментированный код
```
удалил закомментированный код, используемый библиотеку для проверку типов пришедших с сервера данных
```
15. Закомментированный код
```
удалили закомментированный код, для хранения данных в кеше
```